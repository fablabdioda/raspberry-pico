import time
import board
import digitalio

PIN = board.GP25

print("mrug!")

led = digitalio.DigitalInOut(PIN)
led.direction = digitalio.Direction.OUTPUT

while True:
    led.value = True
    print("mrug!")
    time.sleep(0.5)
    led.value = False
    time.sleep(0.5)
